---
description: Markdown 筆記風格規範，適用於 src/content/drafts 下的草稿文章
globs: src/content/drafts/**/*.md
alwaysApply: false
---

# Markdown 筆記風格

## Frontmatter（必填）

```yaml
---
title: 用中文寫的簡潔標題
date: YYYY-MM-DD
description: 一句話說明這篇在解決什麼問題。
tags: [TechTerm1, TechTerm2, 中文tag]
---
```

## 文章結構（固定節奏）

1. **開場段落**（無標題）：一到兩句話建立脈絡，不用 H1
2. `## 問題：<具體問題名稱>` — 描述痛點
3. `## 解法：<技術/工具名稱>` — 直接給答案
4. `## 實際範例` — 帶 file path 註解的程式碼區塊
5. `## 一句話總結` — 結尾摘要表格

較長筆記（多主題）用 `---` 分隔段落，結尾改用 `## 重點整理` 表格。

## 語言混用

- 說明、連接詞 → **中文**
- Package 名稱、API、prop、路徑、程式碼 → **英文原文**
- 術語首次出現：中文說明後括號附英文（例：累積版面位移（CLS））

## 程式碼區塊

```tsx
// path/to/file.tsx  ← 第一行必須有路徑註解
```

- 一律加語言標記（`tsx`, `ts`, `bash`, `yaml` 等）

## 摘要表格

```markdown
| 主題 | 核心機制 |
|---|---|
| `package` | 一句話說明 |
```

不超過 5 列。

## 檔名命名規則

格式：`<主題關鍵字>-<副主題或動詞>.md`，全部小寫，以 `-` 連接。

| 情境 | 範例檔名 |
|---|---|
| 單一技術概念 | `nextjs-image-optimization.md` |
| 解決特定問題 | `fix-gfm-table-rendering.md` |
| 功能介紹 | `nextjs-layouts-and-pages.md` |
| 工具/套件筆記 | `clsx-tailwind-merge.md` |

命名原則：
- 用**英文**，不用中文或拼音
- 反映**筆記的核心問題或技術**，不用泛泛的名稱（❌ `notes.md`、`memo.md`）
- 技術名稱保留原始大小寫慣例的**小寫版**（`nextjs`、`tailwind`、`react`）
- 動詞用原形（`fix`、`optimize`、`setup`），不加 `-ing`

## 禁止

- 不寫廢話過渡句（「在本文中，我們將...」）
- 有 frontmatter 的筆記不加 H1
- 不用 `####` 以下的標題層級
