---
description: 強制執行 Conventional Commits 與 Gitmoji 的提交規範，並定義專案範疇
globs: 
alwaysApply: false
---

# Commit 訊息規範 (Conventional Commits + Gitmoji)

你是一個專業的前端開發助手。當使用者要求你撰寫 Git Commit Message 或進行代碼提交建議時，請嚴格遵守以下規範。

## 1. 訊息格式
每個 Commit 必須遵循以下結構：
```text
<emoji> <type>(<scope>): <subject>

[optional body]

[optional footer]
```

## 2. 類型與視覺標籤 (Types & Gitmojis)

| 標籤 | 類型 | 使用時機 |
|------|------|----------|
| ✨ | feat | 新增功能 (Features) |
| 🐛 | fix | 修正 Bug (Bug Fixes) |
| 💄 | style | UI 樣式、CSS、Tailwind 調整（不影響邏輯） |
| ♻️ | refactor | 程式碼重構（非功能新增也非 Bug 修正） |
| 📝 | docs | 文件更新 (Documentation) |
| ⚡️ | perf | 效能優化 (Performance) |
| ✅ | test | 新增或修正測試案例 (Tests) |
| 🔧 | chore | 雜項、設定檔更新、套件安裝 (Chore) |
| 🚀 | build | 建置系統、部署相關變更 |
| ⏪ | revert | 還原先前的提交 |

## 3. 範疇定義 (Scopes)

請根據變更內容選擇最貼切的範疇：

- `blog`: 部落格文章處理、Markdown 解析
- `ui`: React 元件、頁面佈局、響應式設計
- `config`: 專案配置 (Tailwind, TS, Vite, ESLint)
- `seo`: Metadata, Sitemap, 結構化資料
- `api`: API 串接、Zustand 狀態管理、資料處理
- `auth`: 登入、權限、Token 管理

## 4. 撰寫規則

- **語言**: `<subject>` 必須使用**英文**。
- **動詞開頭**: 使用現在式祈使句，如 `add` 而非 `added` 或 `adds`。
- **大小寫**: 主旨全小寫，結尾不加句點。
- **長度限制**: 主旨請控制在 50 個字元以內。
- **內容專注**: 每個 Commit 只做一件事（Atomic Commit）。
- **說明 Why**: 若有複雜邏輯，必須在 body 解釋變更原因。

## 5. 正確範例

```text
✨ feat(blog): add markdown rendering with syntax highlight
🐛 fix(ui): resolve mobile navbar overflow issue
💄 style(ui): adjust hero section spacing for tablet screens
🔧 chore(config): add vercel deployment configuration
♻️ refactor(api): simplify gemini api response handler
```

## 6. AI 指導語 (AI Instructions)

當我要求你「幫我寫 commit」時：

1. 分析 `git diff` 中的變更。
2. 根據上述表格選取正確的 emoji 與 type。
3. 判斷受影響的 scope。
4. 生成符合規則的英文主旨。
5. 若偵測到多個不相關的變更，請主動建議拆分提交。